Gap Analysis Table
#	Statement from Spec	Current Status	Delta / Gap
S1	StateStore.save/load/clear keyed on tenant_id (table: arb_states)	✅ Implemented (with naming differences)	Key column is user_id (auth.uid), table is fra_state. Functionally equivalent; auth.users replaces a separate tenants table.
S2	save() upserts a single JSONB blob	✅ Implemented + Extended	Also upserts flat version and is_running columns, and mirrors to fra_positions / fra_pnl_history (best-effort).
S3	load() fetches JSONB by tenant, returns null if missing	✅ Implemented + Extended	Also seeds currentLocalVersion in memory; version guard discards stale schema rows and auto-calls clear().
S4	clear() deletes single arb_states row	✅ Implemented + Extended	Cascades deletes across fra_state, fra_positions, fra_pnl_history.
S5	Engine boot: new ArbEngine(config, adapter, store) → engine.restore() → engine.start()	✅ Implemented (different pattern)	Uses MemoryStore server-side (not SupabaseStateStore), pre-loads via store.save(persisted), then engine.start() + engine.tick(). No restore() call — loading is manual. manualTick: true replaces continuous engine.start().
S6	buildAdapter(tenant.exchange, tenant.apiCredentials)	✅ Implemented	Called loadAdapter(). Reads fra_user_exchanges.vault_secret_name → decrypts from vault.decrypted_secrets. Falls back to dry-run adapter if no keys.
S7	Per-tenant Realtime broadcast: supabase.channel('tenant:tenantId').send(...) inside engine.subscribe()	⚠️ Partially implemented — different direction	Server does not push to tenant channels. Instead, fra_state Postgres row is published via supabase_realtime (0001_init.sql L94), and the client subscribes to postgres_changes on fra_state filtered by user_id (use-supabase-fra.tsx L69-101). Equivalent result, push direction is reversed.
S8	Bug 1 fix: catch block resets phase to 'monitoring' on failed close	⚠️ Not in harness — core dependency	No explicit phase-reset in harness. Version guard (L345) reduces but does not eliminate risk. Depends on whether core package carries the fix. Gap if core is unfixed.
S9	Bug 3 fix: update({ phase: 'idle' }) before persistNow() in stop()	⚠️ Not in harness — core dependency	Fix must live in core stop(). Harness mid-tick guard (L340-343) prevents overwriting a user's manual stop, which is the downstream symptom, but doesn't enforce the ordering.
S10	Bug 5 fix: await Promise.all([...engines].map(e => e.stop())) before process.exit(0)	✅ N/A — architecture eliminates bug	Stateless Deno Edge Functions have no in-memory tenant engine pool and no SIGTERM surface. Each tick writes state synchronously before returning. Bug class doesn't exist here.
S11	Bug 4 fix: live-status re-check inside closeAllPositions() loop	⚠️ Partially in harness — core dependency	Per-tenant DB lock (fra_try_lock, L152-173) prevents concurrent server ticks for same user. The position re-check must be in core closeAllPositions(). Lock is a complement, not a substitute.
S12	HyperliquidWebSocket / OKXWebSocket barrel-importable for streaming	❌ Not implemented	Neither class is imported or used. Harness is tick-based (pg_cron → edge function), not streaming. Real-time delivery is via Postgres Realtime, not WebSocket channels. Gap if low-latency streaming is needed.
S13	LocalStorageStore has no server-side role	✅ Correctly implemented	Server uses MemoryStore; client uses SupabaseStateStore. LocalStorageStore never referenced in SaaS layer.
S14	clearPersistedState wired to an admin HTTP endpoint	⚠️ Partially implemented	store.clear() exists and is called on version-mismatch auto-clear. No dedicated admin endpoint. Only key revocation (revoke-exchange-key) has an HTTP surface.
S15	tenants table with exchange, apiCredentials, config columns	✅ Implemented differently	No tenants table. Split across auth.users (identity), fra_state.state (config via JSONB), fra_user_exchanges (exchange preference + vault reference). Functionally equivalent with better separation of concerns.
S16	Subscription enforcement filtering eligible tenants	✅ Implemented + extended	getActiveSubscriberIds() (L69-92) caches active/trialing user IDs with dual TTL (45s positive, 5s negative). eligible = allRows.filter(r => activeIds.has(r.user_id)) (L144). Metrics row logged per tick (L180-188, fra_engine_metrics).
S17	Global + per-tenant concurrency control	✅ Implemented (beyond spec)	Global lock fra-engine-tick (TTL 120s, L116-124) + per-tenant lock fra-tick:userId (TTL 90s, L152-158). Backed by fra_engine_locks table (0004 migration) — TTL-based, not advisory, so it survives Deno isolate crashes.
S18	Mid-tick stop guard (user toggles engine off during tick)	✅ Implemented beyond spec	L335-343: re-reads is_running from DB mid-tick before writing back. Aborts write if user stopped the engine during the tick window. Spec does not mention this.
S19	Version-based write optimization	✅ Implemented beyond spec	L345: only writes fra_state if updated.version !== row.version. Prevents pointless DB writes on ticks that produced no state changes.
Summary of Actionable Gaps
Three items require work; the rest are implemented or N/A:

Priority	Gap	Action Required
High	S8: Bug 1 — phase stuck 'exiting'	Verify the fundrates-arb core package carries the catch-block fix. If not, add a harness-level guard: after engine.tick(), inspect store.load().phase; if 'exiting', log and skip the write-back to avoid persisting the stuck state.
High	S11: Bug 4 — duplicate close orders	Verify core closeAllPositions() has the live-status re-check. The per-tenant lock covers server-tick race only; client-initiated closeAllPositions() from the dashboard racing a cron tick is not covered by the lock.
Medium	S14: Admin clearPersistedState endpoint	Add an admin-only edge function (service role + admin JWT check) that calls store.clear() for a given user_id. Prevents operators from needing raw Supabase access for factory resets.
Low	S12: WebSocket streaming	Not a gap unless low-latency data streaming is a requirement. Current Postgres Realtime delivers updates within ~1s, which is sufficient for 60s tick intervals. Implement HyperliquidWebSocket / OKXWebSocket only if sub-second funding rate updates are needed.
Low	S9: Bug 3 — stop() phase ordering	Verify in core. If the FundingRateArb component calls stop() client-side via the browser engine, and the core hasn't fixed the ordering, the Supabase row can be left with phase: 'monitoring' + isRunning: false. Not currently enforced at the harness level.